
** JSX **


No es lenguaje HTML pero parece serlo con todas la funcionalidad de JS


** Insertar expresiones **

Podemos declarar una variable y luego utilizar con JSX envolviendola entre {}

ej:

        const name = "Jorge"
        const element = <h1>Hola, {name} </h1>

        ReactDom.render(
            element,
            document.getElementById('root')
        );


Esto va a devolver Hola, Jorge



Dado que JSX es mas cercano a JS que a HTML utiliza la sintaxis de camelCase y atributos

como class se vuelven className